# TODO: テーマと設定の分離

- [ ] カラーモードはローカル管理に統一
  - [ ] ThemeProviderをlocalStorage優先（なければシステム）にし、サーバー設定とは切り離す
  - [ ] 初回ちらつき防止のため、head内スクリプトで事前に`dark`クラスを付与
  - [ ] 設定画面ではUIテーマ設定をローカル保存として明示（サーバー保存しない）

- [ ] サーバー保存設定はテーマ以外に限定
  - [ ] Settings API/画面からサーバー側のテーマ設定フィールドを除外
  - [ ] autoplayなど必要な設定のみサーバー保存とし、ラベルを明確にする

- [ ] 主要UIをカラー変数ベースに置換
  - [ ] 背景/テキスト/ボーダーを `surface/card/text/muted/border/accent` で統一（ライト/ダーク両対応）
  - [ ] 入力/ボタン/カードの固定色を変数参照に置き換え、ライトモードでも視認性を確保
